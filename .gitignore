# Miscellaneous
*.class
*.log
*.pyc
*.swp
.DS_Store
.atom/
.build/
.buildlog/
.history
.svn/
.swiftpm/
migrate_working_dir/

# IntelliJ related
*.iml
*.ipr
*.iws
.idea/

# The .vscode folder contains launch configuration and tasks you configure in
# VS Code which you may wish to be included in version control, so this line
# is commented out by default.
#.vscode/

# Flutter/Dart/Pub related
**/doc/api/
**/ios/Flutter/.last_build_id
.dart_tool/
.flutter-plugins
.flutter-plugins-dependencies
.pub-cache/
.pub/
/build/

# Symbolication related
app.*.symbols

# Obfuscation related
app.*.map.json

# Android Studio will place build artifacts here
/android/app/debug
/android/app/profile
/android/app/release

# Firebase-specific & Sensitive Files
android/app/google-services.json
ios/Runner/GoogleService-Info.plist
lib/firebase_options.dart
firebase_app_id_file.json  # If this exists in your project
.firebase/
# Optionally, for Firebase Hosting/Functions (as needed):
.firebase/
firebase-debug.log
firebase-debug.*.log
.runtimeconfig.json
web/firebase-config.js  # If using Firebase for web
These files typically contain sensitive credentials or are generated by Firebase tools and should not be tracked in version control.

Git Commands to Remove Previously Tracked Files
If you've already committed any of these files, simply adding them to .gitignore won't work. To remove them from tracking (without deleting them locally):

bash
git rm --cached android/app/google-services.json
git rm --cached ios/Runner/GoogleService-Info.plist
git rm --cached lib/firebase_options.dart
git rm --cached firebase_app_id_file.json
git rm --cached -r .firebase
git rm --cached firebase-debug.log
git rm --cached firebase-debug.*.log
git rm --cached .runtimeconfig.json
git rm --cached web/firebase-config.js
(Run only the relevant commands for the files present in your repo.)

Then commit these changes:

bash
git commit -m "Remove sensitive/generated files from tracking"
Summary:
Add the above patterns to your .gitignore and
